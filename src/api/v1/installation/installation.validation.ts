import { body } from "express-validator";

// export const addConfigDataValidation = [
//   body("configData").notEmpty().withMessage("configData is required"),
// ];

// // check if the envData is an object
// export const validateUpdateEnvData = [
//   body("envData")
//     .notEmpty()
//     .withMessage("envData is required and must be an object"),
// ];
